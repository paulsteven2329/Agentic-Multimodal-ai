jobs:
  run-groq-model:
    runs-on: ubuntu-latest
    steps:
      - name: Run my script
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}  # This pulls the secret securely
        run: python my_script.py
